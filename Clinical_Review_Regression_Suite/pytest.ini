[pytest]
pythonpath = src
testpaths = tests
addopts = -q --strict-markers
markers =
    live: executes against the deployed Streamlit application
    safety: safety and prompt-injection regression tests

